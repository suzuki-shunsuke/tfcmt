---
name: test
on: pull_request
permissions: {}
jobs:
  test:
    uses: suzuki-shunsuke/go-test-full-workflow/.github/workflows/test.yaml@fix-remove-fix
    with:
      aqua_version: v2.43.0
      golangci-lint-timeout: 120s
    permissions:
      pull-requests: write
      contents: read
